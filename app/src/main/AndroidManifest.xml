<?xml version="1.0" encoding="utf-8"?>
<!-- TODO 01. Change the package name of the app. New package name: com.ualr.firstapp" -->
<!-- TODO 02. Change the app name. New app name: First App -->
<!-- TODO 03. Change the app icon. The new one might use the ic_country.svg image provided as foreground and a solid black color as background-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ualr.helloworld">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>